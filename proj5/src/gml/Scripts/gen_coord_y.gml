/*
 * double gen_coord_y(double y);
 * 
 * Arguments:
 *     y - Particle Coordinate on Y-Axis
 *
 * Description:
 *     Converts the particle's Y coordinate to coordinates that would fit on
 *     the grid drawn on the screen.
 */

return (room_height / 2) - (argument0 * obj_simulator.cell_size);

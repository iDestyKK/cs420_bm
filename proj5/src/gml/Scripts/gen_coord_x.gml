/*
 * double gen_coord_x(double x);
 * 
 * Arguments:
 *     x - Particle Coordinate on X-Axis
 *
 * Description:
 *     Converts the particle's X coordinate to coordinates that would fit on
 *     the grid drawn on the screen.
 */

return (room_width / 2) + (argument0 * obj_simulator.cell_size);
